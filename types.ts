
export interface VoteOption {
  id: string;
  name: string;
  votes: number;
  color: string;
}

export interface ThemeColors {
  background: string;
  text: string;
  primary: string;
  accent: string;
}
